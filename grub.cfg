# ZevOS GRUB fallback configuration.
# Keep GRUB on the firmware/legacy text console so UEFI QEMU does not
# require a graphical GRUB video mode.
set timeout=5
set default=0
set gfxmode=text
terminal_output console

menuentry "ZevOS" {
    multiboot2 /boot/kernel.elf
    boot
}
